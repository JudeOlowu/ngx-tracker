[package]
name = "ngx_screener"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.35", features = ["full"] }
reqwest = { version = "0.11", features = ["json"] }
scraper = "0.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
csv = "1.3"
colored = "2.1"
comfy-table = "7.1"
dotenv = "0.15"
anyhow = "1.0"
thiserror = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
clap = { version = "4.4", features = ["derive"] }
tokio-retry = "0.3"

# NEW — required for Railway server mode:
axum = "0.7"
tower = { version = "0.4", features = ["full"] }
tower-http = { version = "0.5", features = ["cors", "set-header", "fs", "trace"] }

# These are already in your project — confirm they are present:
# tokio = { version = "1", features = ["full"] }
# reqwest = { version = "0.11", features = ["json"] }
# serde_json = "1"
# anyhow = "1"
# tracing = "0.1"
# tracing-subscriber = "0.3"
# clap = { version = "4", features = ["derive"] }

# ================================================================
# FULL Cargo.toml [dependencies] block for reference:
# ================================================================
# [dependencies]
# anyhow = "1"
# axum = "0.7"
# chrono = { version = "0.4", features = ["serde"] }
# clap = { version = "4", features = ["derive"] }
# csv = "1"
# dotenv = "0.15"
# reqwest = { version = "0.11", features = ["json"] }
# serde = { version = "1", features = ["derive"] }
# serde_json = "1"
# tokio = { version = "1", features = ["full"] }
# tower = { version = "0.4", features = ["full"] }
# tower-http = { version = "0.5", features = ["cors", "set-header", "fs", "trace"] }
# tracing = "0.1"
# tracing-subscriber = { version = "0.3", features = ["env-filter"] }
